import React from "react";

import Directions from "../pages/Directions";

export default {
  title: "directions",
};

const directionsTemplate = () => <Directions />;

export const DirectionStory = directionsTemplate.bind({});
